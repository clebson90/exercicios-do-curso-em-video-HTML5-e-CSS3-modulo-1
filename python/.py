from math import trunc
num1 = float(input("Qual o número?"))
print(f"O número inteiro é: {trunc(num1)}")

from datetime import date
print(date.today().year)

from datetime import date
print(date.today())